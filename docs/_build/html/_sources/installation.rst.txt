Installation
============

To install LocalSearch, use pip:

.. code-block:: bash

    pip install LocalSearch

Requirements:
- Python 3.10+
- Optional: Faiss (for vector store), SentenceTransformers (for embeddings)
